{
  "name": "walle-register",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "start": "cd apps/backend && python -m uvicorn app.main:app --port ${PORT:-8000}"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
